<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.zoetermeer.onszoetermeer">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activities.LogoScreen"
            android:launchMode="singleInstance"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Home"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activities.Vitaliteit"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activities.Login"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activities.Registration"
            android:launchMode="singleInstance"
            android:noHistory="true"/>
        <activity
            android:name=".activities.Contact"
            android:label="@string/contact_title_bar"
            android:launchMode="singleInstance" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activities.Chat"
            android:label="@string/title_activity_chat" />
        <activity
            android:name=".activities.Challenges"
            android:launchMode="singleInstance"
            android:noHistory="true"/>
        <activity
            android:name=".activities.PasswordRecovery"
            android:launchMode="singleInstance"
            android:noHistory="true"/>
        <activity
            android:name=".activities.Achievements"
            android:launchMode="singleInstance"
            android:noHistory="true"/>
        <activity
            android:name=".activities.ChallengeDetails"
            android:launchMode="singleInstance"
            android:noHistory="true" />
        <activity
            android:name=".activities.Help"
            android:launchMode="singleInstance"/>
        <activity
            android:name=".activities.RequestDetails"
            android:launchMode="singleInstance"
            android:noHistory="true" />
        <activity
            android:name=".activities.Base"/>
        <activity
            android:name=".activities.Profile"
            android:label="@string/title_activity_profile"
            android:parentActivityName=".activities.Home"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="nl.zoetermeer.onszoetermeer.activities.Home" />
        </activity>
    </application>

</manifest>